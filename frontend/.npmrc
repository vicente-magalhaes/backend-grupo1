# Rede atrás de proxy/antivírus que inspeciona HTTPS pode derrubar downloads grandes.
# Estes ajustes tornam o npm mais resiliente (mais tentativas / timeouts maiores).
fetch-retries=5
fetch-retry-maxtimeout=120000
fetch-timeout=600000
