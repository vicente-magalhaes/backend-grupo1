# ==========================================
# CNH Connect - Backend Dependencies
# Versões fixadas para reprodutibilidade (evita "na minha máquina funcionou").
# ==========================================

# ── Framework ────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0

# ── Supabase ─────────────────────────────
supabase==2.11.0

# ── Configuração e Validação ─────────────
pydantic-settings==2.7.1
python-dotenv==1.0.1

# ── Segurança (auth própria: hash de senha + JWT) ──
bcrypt==4.2.1
pyjwt==2.10.1
python-multipart==0.0.20

# ── Testes ───────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1

# ── Qualidade de Código ──────────────────
ruff==0.9.2
